import styled from 'styled-components'
import { PLAYER } from '../../constants'

export const Wrapper = styled.div`
  width: ${PLAYER.WIDTH * 20}px;
  height: ${PLAYER.HEIGHT * 15}px;
  margin: 10px auto;
  border: 3px solid white;
`
